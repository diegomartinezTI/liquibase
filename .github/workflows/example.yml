on: [push]

jobs:
  test-liquibase-action:
    runs-on: ubuntu-latest
    name: Test Liquibase Action
    steps:
      - uses: actions/checkout@v2
      - uses: liquibase/liquibase-github-action@v2
        with:
          operation: 'update'
          classpath: "example/mysql-connector-java-8.0.17.jar" 
          changeLogFile: 'example/samplechangelog.sql'
          username: "u632645567_pruebas"
          password: "Diego123*-*"
          url: "jdbc:mysql://sql542.main-hosting.eu:3306/u632645567_pruebas"
